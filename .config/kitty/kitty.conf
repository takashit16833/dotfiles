# Kitty の設定入口。
# 役割ごとの設定を分離し、このファイル自体は読み込み順だけを管理する。
include appearance.conf
include keybindings.conf

# マシン固有の設定は ~/.config/kitty/local.conf に置く。
# ファイルが存在しない環境では何も読み込まず、共通設定だけで起動する。
globinclude local.conf
