# Finder の「整頓」に相当する操作を不要にするため、常に自然順で directory を先に並べる。
[mgr]
sort_by = "natural"
sort_sensitive = false
sort_reverse = false
sort_dir_first = true
linemode = "mtime"
show_hidden = false
show_symlink = true
scrolloff = 5

# テキストの preview は横に長い行も画面内で読めるよう折り返す。
[preview]
wrap = "yes"
